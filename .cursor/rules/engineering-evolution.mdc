---
description: 工程原则：可扩展性、可维护性、长期演进；先设计后实现、配置化、多 Provider、可测试性
alwaysApply: true
---

# 可扩展与可维护工程原则（长期演进优先）

在编写代码、设计方案、思考实现路径时，必须优先考虑系统的可扩展性、可维护性与长期演进能力，而不是只追求当前功能可用。若与短期交付冲突，优先利于后续维护与扩展的实现，除非用户明确要求一次性原型。

## 1. 先设计，后实现

- 动手前梳理：整体方案、模块边界、依赖关系、数据流、扩展点。
- 先给出简要实现计划，再编码；避免边写边改、缺乏结构的临时实现。

## 2. 架构优先，避免堆砌

- 功能纳入统一框架与清晰分层；避免耦合、重复、硬编码、补丁式开发。
- 模块职责单一、接口清晰、依赖可控。
- 新功能优先通过扩展现有模块或抽象层接入，避免侵入式大改核心逻辑。

## 3. 禁止硬编码配置

以下不得写死在业务代码中，须通过配置文件、环境变量、配置中心或统一配置模块管理：

- API Key、模型名称、Base URL、Provider 类型、环境地址
- 开关、阈值、重试次数、超时时间
- 文件路径、存储位置、端口号等

## 4. 多 Provider / 多模型兼容（自设计阶段起）

- LLM、图片模型、第三方服务不得按单一供应商写死。
- Key、模型配置、调用方式、参数映射须预留多供应商扩展；Provider 切换尽量仅改配置。
- 优先统一抽象（如 provider adapter / strategy），便于接入 OpenAI、Anthropic、Gemini、国内模型、OpenAI-compatible 等。

## 5. 优先复用成熟方案

- 能用成熟、稳定、社区验证的开源或框架则不自研重复轮子。
- 引入依赖前评估：维护活跃度、生态、可替代性、学习与封装成本；仅当现成方案明显不足时再自研。

## 6. 显式考虑扩展点

对每个核心模块自问：是否会新增实现方式、新平台/模型/数据源、更换底层提供者、不同运行模式。易变处抽象，稳定处保持简单；避免过度设计，也不零预留。

## 7. 可维护性

- 清晰命名、合理目录、明确模块边界；单模块不过度承担职责。
- 减少隐式依赖；避免魔法值、全局污染、跨模块强耦合。
- 公共能力沉淀为可复用模块，不散落复制。

## 8. 可测试性

- 设计时考虑单测、集成测与调试便利；核心逻辑与具体 UI、框架、外部服务解耦。
- 外部依赖可 mock、可替换。

## 9. 设计模式：适用而非滥用

- 示例：Strategy（多模型/Provider）、Adapter（异构第三方）、Factory（多实现创建）、DI（解耦依赖）、Template Method / Pipeline（流程编排）。
- 目标为降耦合、提扩展性，不为“显得高级”而套模式。

## 10. 输出须体现工程思考

实现时优先说明：为何如此设计、模块划分、抽象位置、后续如何扩展、何处复用成熟方案。避免只给能跑代码而无工程说明。

## 11. 默认开发倾向（口诀）

- 先方案，后编码；先抽象边界，后填细节；先配置化，后业务化。
- 先复用成熟能力，后考虑自研；先保证可维护与扩展，后追求短期速度。
